# URL base da API do Zuno (src/interfaces/api). Sem essa variável, o app usa http://localhost:3000.
NEXT_PUBLIC_API_URL=http://localhost:3000

# Login real a partir da Sprint 05 — não há mais tenant de desenvolvimento fixo no frontend.
# Crie um usuário com `npm run db:seed-user` (raiz do projeto) e faça login normalmente em /login.
